export const enum Assets {
    SKY = 'sky',
    GROUND = 'ground',
    BALL = 'ball',
    PLAYER = 'player'
};

export const enum GameObjectsEnum {
    GROUND = 'ground',
};

export const enum Dimensions {
    WIDTH = 800,
    HEIGHT = 600
};